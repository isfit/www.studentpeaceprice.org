<div class="da-slide">
  <h2><%= raw use_english? ? article.title_en : article.title_no %></h2>
  <p><%= use_english? ? article.ingress_en : article.ingress_no %></p>
  <%= link_to use_english? ? "Click here" : "Klikk her", article_path(article), class: "da-link" %>
  <div class="da-img">
    <%= image_tag article.spp_article_image.url(:article) %>
  </div>
</div>
